# Not used yet — src/api/taskService.ts is a mock backed by localStorage.
# Once you swap it for real fetch() calls (see the comment at the top of
# that file), read the API's base URL from here instead of hardcoding it.
VITE_API_BASE_URL=http://localhost:3000/api
